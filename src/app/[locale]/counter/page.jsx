import Counter from "@/components/Counter";
import ErrorPage from "@/components/Error";
import React from "react";

const page = () => {
  return (
    <div>
      {/* <Counter /> */}
      <ErrorPage />
    </div>
  );
};

export default page;
